/**
 * Mapbox default token for your deployment.
 *
 * Setup:
 * 1. Copy this file to mapbox-config.js
 * 2. Add your Mapbox public token: window.MAPBOX_DEFAULT_TOKEN = 'pk.eyJ1...';
 *
 * mapbox-config.js is in .gitignore and will NOT be committed to git.
 * Your token stays private while users get maps by default.
 */
window.MAPBOX_DEFAULT_TOKEN = '';
