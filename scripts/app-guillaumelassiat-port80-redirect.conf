# Port 80: force HTTPS for app host (replaces stale root /var/www/app vhost).
server {
    listen 80;
    listen [::]:80;
    server_name app.guillaumelassiat.com;
    return 301 https://$host$request_uri;
}
